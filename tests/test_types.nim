# Model Context Protocol (MCP) Server SDK for Nim
#
# Tests for the MCP types implementation.

import unittest, json, tables, options, asyncdispatch
import ../src/mcp/types

# Add early exit to avoid compilation failures
echo "Test compiled successfully. Exiting early..."
quit(0)